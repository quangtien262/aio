@php $slides = collect($block['dynamic_items'] ?? [])->whenEmpty(fn () => collect($content['slides'] ?? []))->filter(fn ($slide) => is_array($slide) && filled($slide['image'] ?? null))->values(); $benefits = collect($content['benefits'] ?? []); @endphp
<section class="th5-hero" id="{{ $anchor }}" data-landing-block-id="{{ $block['id'] ?? '' }}" data-block-type="hero_slider">
    <div class="th5-hero__slides" data-th5-slider>@foreach($slides as $index => $slide)<article class="th5-hero__slide {{ $index === 0 ? 'is-active' : '' }}" style="background-image:linear-gradient(90deg,rgba(1,31,24,.84),rgba(1,31,24,.06)),url('{{ $slide['image'] }}')"><div class="th5-container"><div><span>{{ $slide['kicker'] ?? $data['subtitle'] ?? '' }}</span><h1>{{ $slide['title'] ?? $data['title'] ?? '' }}</h1><p>{{ $slide['summary'] ?? $data['description'] ?? '' }}</p><a href="{{ $slide['link_url'] ?? '#bo-suu-tap' }}">{{ $slide['button_label'] ?? $data['button_label'] ?? 'Khám phá ngay' }} <i class="fa-solid fa-arrow-right"></i></a></div></div></article>@endforeach</div>
    @if($slides->count() > 1)<div class="th5-hero__dots">@foreach($slides as $index => $slide)<button class="{{ $index === 0 ? 'is-active' : '' }}" type="button" data-th5-slide="{{ $index }}" aria-label="Slide {{ $index + 1 }}"></button>@endforeach</div>@endif
    <div class="th5-benefits"><div class="th5-container"><div class="th5-benefits__track" data-th5-benefits>@foreach($benefits as $item)<article><i class="{{ $item['icon'] ?? 'fa-solid fa-gem' }}"></i><span><strong>{{ $item['title'] ?? '' }}</strong><small>{{ $item['summary'] ?? '' }}</small></span></article>@endforeach</div></div></div>{!! $editButton !!}
</section>
