@if($paginator->hasPages())<nav class="book20-pagination" aria-label="@themeT('inner.pagination', 'Phân trang')">@if($paginator->previousPageUrl())<a class="book20-button secondary" rel="prev" href="{{ $paginator->previousPageUrl() }}">@themeT('inner.previous', 'Trang trước')</a>@endif<span>{{ $paginator->currentPage() }}</span>@if($paginator->nextPageUrl())<a class="book20-button secondary" rel="next" href="{{ $paginator->nextPageUrl() }}">@themeT('inner.next', 'Trang sau')</a>@endif</nav>@endif
