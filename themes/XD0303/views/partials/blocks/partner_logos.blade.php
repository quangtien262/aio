@php $partners = collect($block['dynamic_items'] ?? [])->whenEmpty(fn () => collect(data_get($content, 'items', [])))->values(); @endphp
<section id="{{ $anchor }}" class="xd3-partners xd-landing-block"><div class="xd3-container xd3-partner-grid">@forelse($partners as $partner)<a class="xd3-partner" href="{{ $partner['href'] ?? $partner['url'] ?? '#' }}">@if(filled($partner['image'] ?? null))<img src="{{ $partner['image'] }}" alt="{{ $partner['alt'] ?? $partner['title'] ?? '' }}">@else<strong>{{ $partner['title'] ?? $partner['name'] ?? 'Đối tác' }}</strong>@endif</a>@empty<div>Chưa có đối tác để hiển thị.</div>@endforelse</div>@if($canEditLanding && filled($block['id'] ?? null))<button type="button" class="xd-edit-block" data-xd-edit-block="{{ $block['id'] }}">Sửa khối</button>@endif</section>
